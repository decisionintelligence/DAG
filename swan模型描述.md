基于 DAG 的双重改进：融合频域软聚类与外生变量权重学习的增强模型

## 一、背景与动机

DAG 是一种通过时间相关模块和通道相关模块分别建模历史外生变量对未来外生变量、历史外生变量对历史内生变量的影响，并将这些相关性注入到预测过程中，从而充分利用外生变量的时序预测框架。然而，DAG 仍存在两方面局限：
1. **通道维度**：在通道相关性注入时，默认所有通道之间均可交互，容易引入噪声通道的干扰，尤其当通道数量多、相关性稀疏时，预测性能下降。
2. **时间维度**：在外生变量利用上，所有外生变量被同等对待，未能显式区分不同外生变量对内生变量的影响强度，缺乏可解释性且可能引入冗余信息。

为此，我们在 DAG 基础上引入两项核心改进：
- **通道维度**：在通道相关性注入前，采用频域马氏距离对通道进行软聚类，生成稀疏掩码，仅允许语义相似的通道之间进行相关性注入，抑制噪声干扰。
- **时间维度**：在时间相关发现阶段，为每个外生变量学习影响权重，实现外生变量的自适应筛选与加权，提高预测精度与可解释性。

## 二、模型整体架构

改进后的模型整体仍遵循 DAG 的双分支结构，但在两个分支中分别嵌入了新的机制。整体框架如下图所示（结构描述，无图）：

1. **输入**：历史内生变量 \(X^{\text{endo}} \in \mathbb{R}^{N\times T}\)，历史外生变量 \(X^{\text{exo}} \in \mathbb{R}^{D\times T}\)，未来外生变量 \(Y^{\text{exo}} \in \mathbb{R}^{D\times F}\)。
2. **时间相关模块**：
   - **时间相关发现子模块**：输入历史外生变量，采用 patch 嵌入 + Transformer 学习外生变量的时序模式，并在该过程中引入 **外生变量权重模块**，为每个外生变量学习一个可解释的影响权重，同时输出用于注入的查询、键矩阵 \(W_{q'},W_{k'}\) 和预测的未来外生变量 \(\hat{Y}^{\text{exo}}\)。
   - **时间相关注入子模块**：输入历史内生变量，利用注入的 \(W_{q'},W_{k'}\) 与原始注意力融合，预测未来内生变量 \(\hat{Y}^{\text{endo}}_{\text{temp}}\)。
3. **通道相关模块**：
   - **通道相关发现子模块**：输入历史外生变量，通过系列嵌入 + Transformer 学习历史外生变量对历史内生变量的影响，提取用于注入的查询、键矩阵 \(\mathcal{W}_{q'},\mathcal{W}_{k'}\)，并输出对历史内生变量的预测 \(\hat{X}^{\text{endo}}\)。
   - **通道相关注入子模块**：输入未来外生变量，在注入前先经过 **频域软聚类模块**，生成稀疏掩码 \(\mathcal{M}\)，然后在 Correlation Transformer Block 中利用 \(\mathcal{W}_{q'},\mathcal{W}_{k'}\) 与原始注意力加权融合，并应用掩码，预测未来内生变量 \(\hat{Y}^{\text{endo}}_{\text{chan}}\)。
4. **融合与输出**：将时间分支与通道分支的预测结果加权融合，得到最终预测 \(\hat{Y}^{\text{endo}}\)。
5. **损失函数**：包含预测损失 \(L_f\)、时间相关损失 \(L_t\)、通道相关损失 \(L_c\)，并引入可调节的平衡系数。

## 三、核心改进模块详解

### 3.1 时间维度：外生变量权重模块（方案A）

在时间相关发现子模块中，我们为每个外生变量引入一个可学习的标量权重，反映该外生变量对未来外生变量预测的贡献程度，进而影响后续注入的注意力参数。

**设计细节**：
- 对每个外生变量 \(X^{\text{exo}}_i\) 的 patch 嵌入序列 \(S_i \in \mathbb{R}^{M\times d}\)，先进行全局平均池化得到 \(s_i \in \mathbb{R}^d\)。
- 通过一个 MLP 与 sigmoid 激活，输出权重 \(w_i \in [0,1]\)：
  \[
  w_i = \sigma(\text{MLP}(s_i))
  \]
- 将权重作用在 patch 嵌入上：\(S_i' = w_i \cdot S_i\)，再将 \(S_i'\) 送入 Transformer 计算注意力，并提取 \(W_{q'},W_{k'}\)。
- 为了平衡稀疏性与弱信号保留，采用 **软阈值 + 渐进式稀疏** 策略：
  - 在损失中引入稀疏正则项 \(\lambda \|w\|_1\)，且 \(\lambda\) 随训练 epoch 逐渐增大。
  - 对权重应用软阈值操作：\(\tilde{w}_i = \text{ReLU}(w_i - \tau)\)，其中 \(\tau\) 为可学习或固定的小正数，使极低权重归零，但仍保留中等强度信号。
- 训练初期温度较高（或稀疏系数较小），后期逐渐降温，使权重分布从“软”过渡到“硬”。

**效果**：模型可自动识别关键外生变量，降低冗余变量干扰，同时通过渐进式稀疏保留微弱但有益的变量信号。

### 3.2 通道维度：频域软聚类与稀疏掩码

在通道相关注入子模块中，我们引入一个可微的软聚类模块，生成稀疏掩码，仅允许语义相似的通道间进行相关性注入。

**设计细节**：
1. **频域特征提取**：对每个通道的未来外生变量 \(Y^{\text{exo}}_i\) 进行 rFFT，取其振幅作为频域表示 \(f_i \in \mathbb{R}^{F/2}\)（假设 \(F\) 为偶数）。
2. **可学习马氏距离**：定义可学习的半正定矩阵 \(Q = A^T A\)，计算通道间的距离：
   \[
   d_{ij} = (f_i - f_j)^T Q (f_i - f_j)
   \]
3. **相似度与概率矩阵**：将距离转化为相似度 \(c_{ij} = 1/(d_{ij}+\epsilon)\)，并归一化得到概率矩阵 \(P_{ij}\)，表示通道 \(j\) 对通道 \(i\) 有用的概率。
4. **可微采样与掩码生成**：采用 Gumbel-Softmax 进行 Bernoulli 采样，得到二值掩码 \(\mathcal{M}_{ij} \in \{0,1\}\)。训练过程中采用 **渐进式降温**：温度参数 \(\tau_{\text{gumbel}}\) 从较大值（如 5.0）逐渐减小到极小值（如 0.5），使掩码从软连续逐渐过渡到硬离散。
5. **稀疏注入**：在通道相关性注入的注意力计算中，将掩码应用于融合后的注意力分数：
   \[
   S_{\text{fused}} = \alpha \cdot \sigma\left(\frac{QK^T}{\sqrt{d}}\right) + (1-\alpha) \cdot \sigma\left(\frac{Q'K'^T}{\sqrt{d}}\right)
   \]
   \[
   S_{\text{masked}} = S_{\text{fused}} \odot \mathcal{M} + (1-\mathcal{M}) \odot (-\infty)
   \]
   然后对 \(S_{\text{masked}}\) 进行 softmax 后与 \(\mathcal{V}\) 相乘。
6. **残差连接保真**：为防止弱信号完全丢失，引入残差连接：
   \[
   S_{\text{final}} = \beta \cdot S_{\text{masked}} + (1-\beta) \cdot S_{\text{full}}
   \]
   其中 \(S_{\text{full}}\) 为无掩码的原始融合注意力分数，\(\beta\) 可学习或设为固定值（如 0.9）。该设计保证了即使某个弱连接在掩码中被剪除，仍有一部分信息可通过残差路径保留。

**效果**：有效抑制无关通道的噪声，同时通过渐进式降温与残差连接，避免弱信号的完全丢失，在通道数多、相关性复杂的场景下显著提升鲁棒性。

## 四、损失函数与优化

模型的总损失函数沿用了 DAG 的三项损失，但增加了稀疏正则项：

\[
L_{\text{total}} = L_f + \lambda_2 (L_t + L_c) + \lambda_3 \|w\|_1
\]

其中：
- \(L_f\)：最终预测损失（L1 或 MSE）
- \(L_t\)：时间相关损失（未来外生变量预测误差）
- \(L_c\)：通道相关损失（历史内生变量预测误差）
- \(\|w\|_1\)：外生变量权重的 L1 正则，用于促进稀疏性

训练过程中，\(\lambda_3\) 可随 epoch 线性增长（渐进式稀疏），同时 Gumbel-Softmax 的温度参数 \(\tau_{\text{gumbel}}\) 按指数衰减。

## 五、总结与创新点

改进后的模型在 DAG 的基础上，实现了：

1. **外生变量自适应加权**：在时间相关发现阶段为每个外生变量学习影响权重，通过软阈值与渐进式稀疏正则，自动识别关键变量，提升可解释性与预测精度。
2. **通道频域软聚类**：利用可学习马氏距离在频域对通道进行软聚类，生成稀疏掩码，并通过渐进式 Gumbel-Softmax 采样与残差连接，在抑制噪声的同时保留微弱有用信号。
3. **双重稀疏机制**：时间维度与通道维度分别采用不同的稀疏策略，共同作用，既避免了冗余外生变量的干扰，又减轻了无关通道的噪声，实现了更鲁棒的时序预测。

该模型保持了 DAG 端到端训练的特性，同时引入了可解释的权重和可调的稀疏度，适用于外生变量多、通道相关性复杂的实际应用场景（如电力市场、交通预测等）。